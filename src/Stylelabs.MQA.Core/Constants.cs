namespace Stylelabs.MQA.Core
{
    public static class Constants
    {
        public enum AvEnvironment
        { Google }

        public enum AvDriver
        { Chrome, Firefox, Edge, Safari }
    }
}